import React from "react";
import ReactDOM from "react-dom/client";
import App from "../mobileme_fuel_finder_mockup (1).jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
